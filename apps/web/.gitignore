# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files — real values are ignored, the template is committed
.env*
!.env.example
# Committed template for production. The real .env.production holds live
# secrets and stays ignored by the .env* rule above.
!.env.production.example

# cloudflare / opennext
.open-next
.wrangler
.vercel
# generated by `pnpm cf-typegen` from wrangler.jsonc bindings (also in the root ignore)
cloudflare-env.d.ts

# typescript
*.tsbuildinfo
next-env.d.ts
